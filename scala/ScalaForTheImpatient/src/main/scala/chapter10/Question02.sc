package ch10

object Question02 {
  println("Welcome to the Scala worksheet")       //> Welcome to the Scala worksheet
  
  // 2. 通过把 scala.math.Ordered[Point] 混入 java.awt.Point 的方式，定义 OrderedPoint 类。按照辞典编辑方式排序，也就是说，如果 x < x' 或者 x = x' 且 y < y'，则 (x,y) < (x',y')。
}